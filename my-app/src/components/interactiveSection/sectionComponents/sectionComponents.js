import React from 'react';

// This is a component
export const SectionHeader = (props) => {
    return (
        <div>
            {props.text}
        </div>
    )
}

export const SectionDescription = (props) => {
    return (
        <div>
            {props.text}
        </div>
    )
}
